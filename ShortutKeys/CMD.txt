1)cd DIRECTORY_NAME  --> to open a directory in a command prompt.
    then cd FILDER_NAME   --> to open a folder  in cmd
          then cd filename  --> to open a file in command prompt.(type a few then use TAB to get list of files)
       (OR)
   just type cmd on the file path it aotomatically opens the file in the cammnd prompt.
    
2)cd ..    --> to go back to the previous  step.
upper/lower arrow like undo/dedo of commands only(not an action).

3)mkdir FOLDER_NAME    :   to cerate a folder for your project.(cd Desktop --> Desktop/mkdir my_project_folder)

4)ipconif/all  --> to get the ip address 

arrow symbles -->  and <-- to  use previously used commands.

